# Prompt Profiles

CLI:

```bash
python app.py test_fa.mp3 --prompt-profile technical
```

Profiles: general, technical, meeting, lecture.

Dataset benchmark:

```bash
bash scripts/run_dataset_benchmark.sh
```

If Termux closes, run the same command again.

Summary:

```text
output/benchmarks/dataset_safe/summary.json
```
